import {
  "moonbitlang/core/json",
  "justjavac/webview_bootstrap" @bootstrap,
  "justjavac/webview_runtime" @wvrt,
}

supported_targets = "native"

options(
  targets: {
    "config_bridge.mbt": [ "native" ],
    "create.mbt": [ "native" ],
    "install.mbt": [ "native" ],
    "order.mbt": [ "native" ],
    "types.mbt": [ "native" ],
  },
)
