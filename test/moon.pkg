import {
  "moonbitlang/core/json",
  "justjavac/ffi",
  "justjavac/webview_runtime" @runtime,
  "justjavac/webview",
}

warnings = "-29"

supported_targets = "native"

options(
  targets: {
    "extension_host_cases.mbt": [ "native" ],
    "runtime_app_cases.mbt": [ "native" ],
    "webview_smoke.mbt": [ "native" ],
  },
)
